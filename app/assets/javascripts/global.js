$(function(){
  // $('.date').datepicker({format: 'dd-mm-yyyy'}).on('changeDate', function(ev){
  //       console.log('change', ev);
  //       console.log(ev.target.value, $(ev.target).val());

  //   });
})

function isNumber(n) {
  return !isNaN(parseFloat(n)) && isFinite(n);
}